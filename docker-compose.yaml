version: '3.8'

services:
  app:
    build: .
    container_name: local-chat
    ports:
      - "3008:3000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
